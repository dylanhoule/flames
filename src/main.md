# main.tsx

Browser entry point. Mounts `App` into `#root` under React's StrictMode and
pulls in the global stylesheet.

Imports: `react` (StrictMode), `react-dom/client` (createRoot) for React 19's
root API, plus the local `App` component and `index.css`.
