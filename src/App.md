# App.tsx

The application shell. Currently a placeholder that paints the backdrop
gradient; the diorama Scene mounts here in Phase 2.

Imports: `./visual` for the backdrop colours. React is provided by the JSX
runtime rather than imported directly.
